NODE_ENV=development
PORT=5000

# Database Configuration
# Supabase PostgreSQL Database
# TEMP: Using Supabase pooled port to proceed until Docker Desktop is running
DATABASE_URL=postgresql://username:password@localhost:5432/saasdb

# Email Configuration - Gmail (Commented out SMTP Office365)
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USERNAME=dev-saas@primussoft.com
# SMTP_PASSWORD=First@099
# FROM_EMAIL=dev-saas@primussoft.com

# Gmail Configuration (Active)
# Please replace these with your actual Gmail credentials:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
FROM_EMAIL=you@example.com
SMTP_USERNAME=you@example.com
SMTP_PASSWORD=your-app-password
FROM_NAME=SaaS Factory Platform
ADMIN_EMAIL=admin@example.com

# Authorized Platform Admin Emails (comma-separated)
AUTHORIZED_ADMIN_EMAILS=admin@example.com

# JWT Secret for authentication
JWT_SECRET=your-jwt-secret-key-min-32-characters-long

# Allowed return URL prefixes for cross-app redirects (comma-separated)
ALLOWED_RETURN_URL_PREFIXES=http://localhost:5173,http://localhost:5299

# Azure AD Configuration (REAL - from CLI setup - Updated 2025-09-04)
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_REDIRECT_URI=http://localhost:5000/api/platform/auth/azure/callback

# Base URLs for OAuth callbacks (FIXED - consistent port 5000)
BASE_URL=http://localhost:5000
CLIENT_URL=http://localhost:5000
## CORS allowed origins for external demos
CORS_ORIGINS=http://localhost:5173,http://localhost:5299

# Allowed return URL prefixes for cross-app redirects (claims portal, etc.)
ALLOWED_RETURN_URL_PREFIXES=http://localhost:5173,http://localhost:5299
