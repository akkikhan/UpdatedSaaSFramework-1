# Azure Secret Configuration (Moved)

Secret management guidance now lives in `docs/AZURE_SECRET_CONFIG.md`.

Use that document for mapping Azure app secrets to environment variables and key vault rotations.
